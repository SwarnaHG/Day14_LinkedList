package LinkedList1;

public class Node {

        int data;
        Node next;//so this Node is the reference for the next node

        Node(int data)
        {
            this.data = data;
        }
    }
}
